---
execution:
  scenario: simple
  concurrency: 50
  hold-for: 3m

scenarios:
  simple:
    requests:
      - url: 'http://localhost/SimpleTravel/index.php'
        method: GET
      - url: 'http://localhost/SimpleTravel/chooseFlights.php'
        method: POST
        body:
          fromPort: Paris
          toPort: Rome
